// Firebase configuration (Firebase console → Project settings → Your apps → Web app → "Config").
// These values are not secret: access is controlled by firestore.rules and the team's space link.
// Leave as null to use the built-in Node server instead (npm start).
export const firebaseConfig = null;
